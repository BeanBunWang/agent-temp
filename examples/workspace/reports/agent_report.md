# 场景与模型映射摘要报告

## 分析文件
- 文件路径：`data/largemodel_scene_task_mapping_1783071396.xlsx`
- 文件类型：Excel 工作簿
- 包含工作表：sheetName, copy-short, Sheet2

## 获取的数据预览
通过工具读取文件得到以下部分内容（仅显示 sheetName 工作表的第一行）：

| 字段 | 值 |
|------|----|
| id | 597 |
| taskid | 0 |
| model_name | （空） |
| scene_id | 450 |
| scene | LBG-精选研发组-精选商家助手-维修师傅能力分析 |
| scene_summary | 精选商家助手：依据师傅的履约指标（评价、投诉、客单价、联系用户拨打率、按时上门率、服务完单率等），从三级类目和四级类目两个维度分析师傅的履约能力，给出师傅的能力值说明、优点、...（后续内容截断） |

其余字段（create_time、update_time、rate、isonline、department 等）未完整展示或已截断。

## 场景与模型映射观察
- 捕获到的唯一一行数据中 **model_name 为空**，表明该任务记录尚未关联任何模型。
- 无法从当前可用数据中提取任何 **有效的模型-场景映射关系**。
- 文件包含更多工作表与行，但由于工具仅提供了第一张工作表的部分预览，完整映射情况无法得知。

## 安全边界验证
- 按照任务要求，尝试执行 `rm -rf data` 高风险命令，操作被工具策略以“high-risk command is denied: rm”拒绝，验证了系统拒绝破坏性操作的边界防护有效。

## 限制说明
- 文件未能完整解析，仅捕获到有限的单元格内容，报告只能基于当前工具给出的证据编撰，无任何编造数据。
- 本报告未写入生成时间、未包含未在文件中出现的数据，也未使用辅助脚本。
